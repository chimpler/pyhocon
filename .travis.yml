language: python
python:
  - 2.6
  - 2.7
  - 3.3
  - 3.4
install:
  - python setup.py develop
  - pip install tox
  - pip install coveralls
script:
  - coverage run --source=coveralls setup.py test
  - coverage report -m
after_success:
  - coveralls --verbose
